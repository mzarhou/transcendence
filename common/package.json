{
  "name": "@transcendence/common",
  "version": "0.0.1",
  "main": "./dist/index",
  "types": "./dist/index",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "db:generate": "yarn prisma generate --schema=../api/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1",
    "jotai": "^2.4.2",
    "tsup": "^7.2.0",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "prisma": "^5.3.1"
  }
}
